<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Pokemon{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
            <link href="{{ asset('styles/main.css') }}" type="text/css" rel="stylesheet" />
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block header %} 
        <div class="header">
            
            <img class="header__img" src="assets/img/pokeball.png">
            <h2>Pokemon HMG</h2>
        </div>
        {% endblock %}
        {# {% if age >= 18 %}
            <p>{{ name }} eres mayor de edad </p>
        {% else %}
            <p>{{ name }} eres menor de edad </p>
        {% endif %} #}
        {% block content %} CONTENT {% endblock %}
        {% block body %}{% endblock %}
    </body>
</html>
